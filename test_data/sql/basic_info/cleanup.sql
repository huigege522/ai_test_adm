-- ============================================================
-- basic_info/cleanup.sql — 清理 basic_info/baseline.sql 插入的数据
-- 生成日期：2026-05-15
--
-- 【执行目标库 — PolarDB 测试库】
--   - 与 basic_info/baseline.sql 相同；MySQL 侧清理见 shared/cleanup.sql（cmp_apple_market）。
--
-- 说明：
--   1. 按主键 id 精确删除，不使用 TRUNCATE
--   2. 删除顺序：先子表 apple_app_org_attr，再 apple_app_ext，最后 apple_org_ext
--   3. 与 basic_info/baseline.sql 中的 id 区间一一对应
-- ============================================================

-- ── 子表：apple_app_org_attr（702001～702005，702099）────────────────
DELETE FROM apple_app_org_attr WHERE id IN (702001, 702002, 702003, 702004, 702005, 702099);

-- ── 产品扩展：apple_app_ext（701001～701005，701011～701013，701021～701022，701099）──
DELETE FROM apple_app_ext WHERE id IN (
  701001, 701002, 701003, 701004, 701005,
  701011, 701012, 701013,
  701021, 701022,
  701099
);

-- ── 组织扩展：apple_org_ext（700001～700005，700011～700013，700021～700025，700031，700041～700042，700051～700052，700099）──
DELETE FROM apple_org_ext WHERE id IN (
  700001, 700002, 700003, 700004, 700005,
  700011, 700012, 700013,
  700021, 700022, 700023, 700024, 700025,
  700031,
  700041, 700042,
  700051, 700052,
  700099
);
